{
  "name": "client-management-crm",
  "version": "1.0.0",
  "description": "Full-stack MERN CRM project structure",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}